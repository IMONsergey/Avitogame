# Проверка сборки 25.09.2026

Проверена опубликованная игра на https://imonsergey.github.io/Avitogame/ в Chrome.

- Все 10 тестов `npm test` прошли локально и в GitHub Actions.
- Старт, четыре правила, форма и результат визуально сверены с актуальной v10 Figma. Размер исходного холста — 1920 × 1080. Проверены локальные шрифты, SVG и девять прозрачных изображений.
- Пустой рейтинг отображает пустое состояние, без фиктивных участников.
- Имя введено экранной русской клавиатурой, фамилия — обычным вводом.
- Телефон введён экранной цифровой клавиатурой. После удаления цифры кнопка «Далее» блокируется, после восстановления номера разблокируется.
- Пройдена настоящая партия: просмотрено 18 карточек, проверено несовпадение, затем найдены 9 пар. Получен результат 1:17. Таймер остановился, показано предложение повторить попытку для подарка.
- Рестарт из результата возвращает 18 закрытых карточек и 0:00; расположение предметов меняется.
- После перезагрузки страницы результат сохранился в рейтинге.
- В пульте организатора отображаются 1 участник и 1 завершённая попытка. CSV скачан и прочитан: все девять столбцов и результат корректны.
- Ошибок приложения в консоли при прохождении не найдено; наблюдались только сообщения расширения среды проверки.

Тестовые данные существуют только в браузере проверки; в браузерах посетителей рейтинг изначально пустой. Физический тачскрин и отключение сети на площадке не тестировались. Перед мероприятием необходима проверка на целевом устройстве и установка юридических документов в `src/config.js`.

## Обновление 1.1 — проверка на публичной странице

- Все 13 тестов проходят; добавлены проверки получения свежего CSS, офлайн-перехода и защиты рабочей копии от ответа сервера 502.
- Переход через `update.html` обновил старый установленный service worker без закрытия вкладок и вернул на главный экран. Рейтинг сохранился.
- Проверены и сохранены скриншоты новой сетки правил и регистрации. Клавиатура показывается сразу, текст согласия остаётся видимым, новая кнопка доступна после заполнения.
- Имя «Анна» введено виртуальными клавишами, другие поля обычным вводом. Полная партия пройдена через интерфейс, без доступа к скрытому расположению карточек.
- При несовпадении фактические стили обеих открытых карточек: фон rgb(186, 218, 251), рамка rgb(4, 224, 97). Красной заливки и рамки нет.
- Первая найденная пара дала 1/9 и один заполненный сегмент. В DOM появились две подсветки с анимацией и три анимированных сердца. Область эффектов имеет pointer-events:none. После завершения эффекты удалились.
- Отмена выхода сохранила найденную пару и позволила продолжить. Полная партия завершилась при 9/9, девяти сегментах и 18 открытых карточках; результат 2:22.
- Рестарт сбросил таймер до 0:00, прогресс до 0/9, все карточки и эффекты. Подтверждённый выход с открытой карточкой вернул на главный экран. Незавершённая попытка не записалась.
- Пульт после проверки показывает одного участника и две завершённые попытки; лучший результат остался 1:17.

Скриншоты: `rules-v11.jpg`, `registration-v11.jpg`. Проверка выполнена в Chrome; физическое устройство мероприятия не подключено.

## Обновление 1.2 — регистрация, цвета, рейтинги и финал

- 18 автоматических тестов проходят. Дополнительно проверены уникальность девяти цветов Sellers, пустой и частичный рейтинг, топ-10, длинные имена/время и экранирование пользовательских имён.
- Цвета сверены с обязательным Figma Goods Guides B2B / Sellers, палитра 16289:41062. Через настоящий интерфейс открыты все 18 карточек: девять разных цветов. После нахождения пар исходные цвета сохранены; ошибок с красной заливкой нет.
- Пройдены две полные партии новой версии. На завершении — 9/9, таймер остановлен, результат записан. Проверены рейтинг с двумя реальными участниками и длинное двойное имя в финальном топ-3.
- Во время незавершённой партии открыто подтверждение рестарта. Кнопка «Продолжить» сохранила открытую карточку и позволила продолжить игру.
- В предыгровом рейтинге отдельные позиции, крупные номера и время; все десять мест помещаются без прокрутки. Пустые позиции явно отличаются от результатов.
- Проверена широкая форма регистрации с экранной клавиатурой, полями имени и фамилии рядом и стрелкой 426 × 424. Текст согласия совпадает с предоставленным скриншотом.
- После последней пары в DOM появились 42 частицы с действующей анимацией confetti-fall. Анимация медали также активна. Визуально проверен финальный экран с крупным временем и длинным именем.

Скриншоты: `board-v12.jpg`, `registration-v12.jpg`, `result-v12.jpg`. Прогоны выполнялись через публичный интерфейс, без чтения скрытого расположения карточек. Проверка на физической панели остаётся за площадкой.

## Обновление 1.3 — семь скриншотов и аудит движений

- 20 тестов проходят, включая геометрию базового холста и заполнение шести размеров окон без искажения пропорций. У всех девяти цветов лицевых сторон относительная яркость выше 0,58.
- Через публичную служебную страницу проверены реальные iframe 1920 × 1080, 1440 × 900 и 2048 × 1029. Форма и согласие помещаются, клавиатура занимает всю ширину; scrollTop контейнера остаётся 0. Исправлена обнаруженная при проверке автопрокрутка масштабированного контейнера при фокусе.
- Неверный номер проверен кнопкой экранной клавиатуры «Готово»: aria-invalid=true, сообщение «Проверьте номер телефона» расположено внутри поля телефона. После корректного номера кнопка «Играть» активна.
- Пройдена полная партия 9/9 через открытие карточек, без чтения скрытой раскладки. На 26 открытиях зафиксирована opacity=1 у карточки, её обёртки и непрозрачной рубашки; transform внутреннего слоя — none. После завершения эффекта у карточек animation-name=none, раздача не запускается повторно.
- Из интерфейса получены все девять светлых цветов. Рюкзак — #FEEADB; насыщенных оранжевых или красных состояний ошибки нет. Найденные пары сохраняют цвет.
- На базовом холсте отступ времени справа — ровно 24 px. Выход начинается на x=72, рестарт заканчивается на x=1848; размеры обеих кнопок 426 × 96.
- Подтверждение рестарта отменено: осталось 8/9. После последней пары — 18 найденных карточек, 16 частиц вокруг двух карточек, три сердца и кольцо. Финал получил 42 частицы конфетти и backdrop-filter:blur(14px).
- В блоке сообщения под итоговым временем нет иконки. Финал проверен также в 1440 × 900: тексты и кнопки помещаются. Рестарт вернул 18 закрытых карточек и 0/9. Выход через подтверждение вернул на главную.
- Рейтинг содержит заголовок «Самые быстрые», без иконки и дескрипшна, и десять отдельных мест. Длинное имя помещается на две строки.

Скриншоты: `rules-v13.jpg`, `registration-v13.jpg`, `board-v13.jpg`, `result-v13.jpg`, `ranking-v13.jpg`. Браузерная проверка размеров не заменяет тест физического сенсора на площадке.

## Обновление 1.4 — девять новых предметов и правки по шести скриншотам

- 24 теста проходят. Проверены выбор ровно 9 из 18 предметов, две копии каждого, уникальный светлый цвет каждой пары, наличие всех изображений, набор телефона с 8/7/9, удаление и замена цифр, неразрывные пробелы в цепочках коротких слов.
- GitHub Pages успешно опубликовал 78556363c969492b35495c99284db17de80765e9.
- В опубликованной игре проверены кубок в кнопке рейтинга, отсутствие звезды возле примера пары, размер текста про попытки 36 px. Согласие визуально помещается, предлоги объединены с текстом ссылок.
- Экранными кнопками набрано 89000000001: форма показывает +7 (900) 000-00-01, кнопка «Играть» активна.
- Пройдена полная партия через реальные открытия, без чтения скрытой раскладки. Набор: мяч, лампа, чайник, тостер, часы, шуруповёрт, телефон, гитара, наушники. На 8/9 все 16 найденных карточек имеют opacity=1, animation-name=none; галочек в DOM нет. Цвета совпадающих карточек одинаковы.
- Финал показал 9/9 и время 2:33; сохранение прошло, рейтинг открыт. Рестарт сбросил таймер и счётчик; первая открытая карточка — чемодан, отсутствовавший в предыдущем наборе. 3D-переворот сохранён.
- Девять прозрачных WebP 512 × 512 весят суммарно 450154 байта. В сборке 47 файлов; новые изображения и модуль типографики включены в офлайн-кэш.

## Обновление 1.4.1 — стартовая плашка

- На 1920 × 1080 правая плашка занимает x=972, y=72, 876 × 936; низ на y=1008. Отступы сверху, справа и снизу равны 72 px.
- Проверены iframe 1440 × 900 и 2048 × 1029: панель растягивается по сетке, обе карточки остаются внутри. Дополнительно просмотрен обычный браузер 1363 × 936.
- Наблюдались закрытая карточка, её поворот и две открытые карточки с подсветкой. Computed animation — hero-demo-flip, период 9 s; transform внутреннего слоя и лицевой стороны корректно поворачиваются на 180°.
- 24 теста и сборка проходят. Скриншот: `start-v141.jpg`.

## Обновление 1.4.2 — прокрутка полного рейтинга

- 25 автоматических тестов и сборка проходят. Проверены 127 участников, выбор лучшей попытки, позиции после десятой и сохранение компактного топ-3 в результате.
- GitHub Pages опубликовал 3831379e25e86aefd91447bbd7ef5fc0b72dc9d6. Снят лимит в 10 участников; две колонки продолжаются следующими десятками.
- Служебная страница `checks/ranking.html` использует действующие стили и рендерер с 50 явно тестовыми записями, без доступа к хранилищу игроков.
- Прокрутка колёсиком переместила список на 740 px. Клавиша End достигла последнего, 50-го участника; scrollTop равен максимальному значению 3305 px. Заголовок, крестик и нижняя кнопка остались закреплены.
- В окне 1363 × 936 высота строки равна 149,301 px в координатах холста: видны пять крупных строк в каждой колонке. Прокручивается только список; прокрутка обновляет таймер активности.
- Скриншот нижней части тестового списка: `ranking-scroll-v142.jpg`.

## Обновление 1.5 — 18 товаров для дома

- Созданы 18 отдельных генераций встроенным image_gen; перечень, точные промпты и ссылки на разделы брендбука записаны в `generated-products.json`.
- Стиль сверён с Goods Guides B2B / Sellers: 16289:38649 (детализация), 16289:38630 (баланс реализма) и 16289:41062 (палитра).
- Все 18 WebP имеют размер 512 × 512 и прозрачный альфа-канал; общий вес 683014 байт. В папке товаров ровно 18 новых файлов, старый набор удалён из поставки.
- 25 тестов и сборка проходят. Сохранились 9 случайных пар из 18, уникальные светлые цвета, переворот, рейтинг и сохранённые результаты. Собрано 49 файлов, кэш 27e15c15e3e2.
- После публикации в Chrome все 18 изображений на `checks/products.html` загрузились с naturalWidth=512. Просмотрен полный набор на разных пастельных фонах; скриншот `home-products-v15.jpg`.
- Проверены новый предмет на старте и в правилах, регистрация, открытие кресла и закрытие несовпавшей пары в реальной игре. Поворот составляет 180°, непрозрачность карточки остаётся 1.
- В Chrome обнаружено исчезновение содержимого закрытых рубашек при вложенных 3D-слоях. Обе стороны теперь поворачиваются независимо с собственной перспективой; 3D-переворот сохранён. В опубликованном исправлении dd1718512d8114d2554ae27e19e00c006f614936 выполнены семь открытий и три несовпадения за минуту: все 17 закрытых рубашек видимы, открытая тумба загружена. Скриншот `home-flip-v15.jpg`.
- Итоговая сборка: 49 файлов, кэш de7822fa18fc. GitHub Actions и публикация итогового исправления успешны.

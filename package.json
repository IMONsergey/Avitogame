{
  "name": "avito-find-a-pair",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs"
  },
  "engines": { "node": ">=22" }
}
